import React from 'react';

const Axes = ({ axis0, axis1, axis2, axis3 }) => (
	<div>
		<div>{ axis0 }</div>
		<div>{ axis1 }</div>
		<div>{ axis2 }</div>
		<div>{ axis3 }</div>
	</div>
);

export default Axes;
